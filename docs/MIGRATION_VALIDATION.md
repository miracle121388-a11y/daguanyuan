# 迁移验证记录

验证完成于 2026-09-13 11:37（Asia/Shanghai）。环境复现与继续开发步骤见 [MIGRATION.md](MIGRATION.md)，机器可读结果和原始日志索引见 [github-handoff.json](../reports/acceptance/github-handoff.json)。

## 实际验证范围

从 GitHub 独立克隆 `8142c430f548e909d512ae05a3183949b382fff3`，下载完整 Git LFS 素材；待发布的迁移提交随后通过本地 Git 拉取进行验证。实际测试代码为 `bc8ea824441ee86bb5f2093d3accf5c852727e15`，后续提交仅补充本记录和验收日志。

实际运行系统为 Windows x64，使用 Node.js 24.12.0、Python 3.13.9。独立目录新建 Python 虚拟环境并安装 `requirements-dev.txt`，执行 `npm ci`，通过仓库脚本从官方源下载并校验 Blender 4.5.9；没有复用原项目的依赖目录或 Blender 安装。macOS 安装、路径和协作步骤已提供，但本次没有在实体 Mac 上运行。

| 检查 | 实测结果 |
| --- | --- |
| Git LFS 完整性 | `git lfs fsck` 通过 |
| 素材清单 | 1,302 份文件、2,348,451,338 字节，大小与 SHA-256 均通过 |
| 模型资源引用 | 169 个 GLB/glTF、853 个外置资源引用、601 条已审核来源校验通过 |
| Blender 母场景 | 185 张文件图像全部打包，0 个外部链接库，0 个缺失依赖；只读检查，未保存或修改母场景 |
| 代码与数据 | `typecheck`、`lint`、6 项单元测试、`verify` 全部通过 |
| 空间与模型 | 319 项空间检查及 `models:verify` 通过 |
| 构建与打包 | `build`、`deploy:package` 通过，重新生成的上传目录为 49,536,600 字节，启用 Brotli |
| 换行兼容 | Windows 下分别以 `core.autocrlf=true` 和 `false` 检出 4 份代表素材，8 项字节校验通过 |

## 素材补齐与获取方式

9 份归档模型原先缺少的 71 张共享贴图已按原始字节补入仓库，共 3,632,571 字节；来源、模型引用和哈希记录在 [archive-provenance.json](../assets/textures/archive-provenance.json)。现用模型及其贴图均已收录。

未被当前场景采用的候选 `pine_tree_01` 仍有 13 个可选依赖由 `npm run materials:pine` 获取，总计 958,087,934 字节。13 个官方地址已检查可访问，并实际下载、校验其中一张纹理；未下载完整的约 958 MB 候选源素材。冻结的来源元数据和下载脚本在仓库中，下载结果会核对预期大小与 MD5 并记录 SHA-256。

首次迁移检查发现已有 Windows 克隆可能留有旧的 CRLF/LF 差异。`--repair-line-endings` 仅在工作文件与已提交素材内容一致、差别仅为换行时恢复精确字节；实际修复后完整素材检查通过。操作步骤见迁移说明。

本次验证针对文件、开发环境与构建迁移，没有新增浏览器端到端测试或视觉验收，也没有重新部署 Zeabur。生成的发布包已通过检查，但不宣称与历史部署包逐字节相同。旧临时检查点、依赖缓存和本机凭据不属于必要开发材料；环境依赖由仓库中固定版本及获取说明恢复。
