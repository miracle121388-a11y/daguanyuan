# 当前版本的边界 · r14

`spatial-garden-20260912-r14` 已部署到原 California 服务，部署ID `6aa5791dfbf9c810b64d524c`，状态 RUNNING。HTTPS、18 个公开文件哈希及线上桌面／触摸交互检查通过。本机默认连接仍报 `ERR_CONNECTION_CLOSED`；成功的线上检查使用临时公共解析、关闭 QUIC 并保留 TLS 验证。项目外代理修复未实施。本地准确成品为 http://127.0.0.1:4293/ 。

用户要求“快速结束”后，本轮停止进一步打磨。独立复核的原五项评分为：FORM 原始认证证据 unresolved；地被连接与焦点植物 partial；手机近景与月夜纵深 resolved。竹叶分组和枝间空隙改善，但390px画面仍有细碎感；主楼周边种植与土草边缘仍未完全达到参考图质感。本轮未见新增回归。技术检查和发布成功不代表完整视觉通过，更不等于照片级复刻。最终依据为 `reports/acceptance/r14-verdict.md`。

11 项最终端到端测试通过。限速手机模拟首载10.09秒、7.89MiB，条件为390×844、设备DPR3／渲染DPR1、9Mbps、80ms、CPU×4；没有实体手机、Safari、热稳定性或稳定显示FPS结论。r13更详细的回调间隔和绘制统计仅保留作历史，不当作r14数据。空间解释、文学证据、艺术参考与策划导览的区别继续保留。

重复贴图编码已修复：实际19份贴图往返保持字节一致，全部40份模型和贴图重新载入。最终172张运行时栅格资源中，仅几何生成的地表控制图和母场景阴影图改变；其余与此前已验证资源保持一致，一份未引用的生成贴图已清理。历史检查中的PowerShell包装器误把原生命令警告当错误的记录保留，最终完整重跑退出码0。

以下为上一版历史记录；当前结论以本节及r14报告为准。

# 当前已发布版本的边界 · r13

`spatial-garden-20260912-r13` 已在原 Zeabur California 服务运行。部署 `6aa56ca8fbf9c810b64d503f` 的 HTTPS、18 个公开文件哈希和线上桌面／触摸模拟交互已验证。测试浏览器使用临时公共解析并关闭 QUIC，TLS 验证保留；本机默认连接仍报 `ERR_CONNECTION_CLOSED`。已准备本站的 Clash 单域名修复预览，尚未收到授权，未修改本机代理。详见 [部署记录](DEPLOYMENT.md) 与 [访问诊断](ACCESS_DIAGNOSIS.md)。

当前视觉复核结论为 **fix**，只覆盖原有五项问题，没有全页视觉通过结论。地被连接和焦点植物材质均有改善，仍为 partial：主楼附近仍见分离叶簇和宽缓地表过渡；手机竹叶仍显细碎，枝叶层次不足。手机近景与月夜纵深保持 resolved。过暗叶簇回归 R1 已解决。历史 FORM 种子 `41ecba8d` 的原始认证证据仍缺失，不能补造；这与网页功能和原著证据分别记录。见 `reports/acceptance/r13-verdict.md`。

实际完成的检查包括构建、类型、lint、6 项单元、319 项空间关系、3,393 项数据完整性及 11 项端到端测试。手机模拟为 390×844、设备 DPR 3／渲染 DPR 1、9 Mbps、80 ms、CPU ×4：首载 10.17 秒，记录初始资源 7.94 MiB。近院触摸另测 48 次绘制、731,713 三角面，平均回调间隔 16.16 ms、P95 35.20 ms；它是无网络／CPU 限速的 Windows Edge 模拟，不是实体手机、Safari、显示 FPS 或发热测试。详见 [性能条件](PERFORMANCE.md)。

623 个新增原生地被实例沿既有种植区落地，999 个源植物实例复用两份网格；总览与近院分别限额并按需加载。14,146 个原有对象的变换、数据关联及集合关系已检查保留；Manual_Adjustments 保留，当前没有对象。与上一冻结发布包相比，40 个 GLB 未变化，更新来自实例清单、网页叶片受光与几何生成的地表控制图。原生渲染与浏览器薄叶光照近似不等同，不能用原生截图代替手机画面验收。

空间检查证明同一解释方案内的关系一致；绝对坐标、尺度、园外地貌和导览仍属设计解释。文献、空间解释、艺术参考图与策划路线保持区分。交互包含轨道观察、室内预设、揭顶和道路导览，没有任意第一人称墙体碰撞行走。22 个事件为精选内容，没有人类红学专家审定的声称。运行资源由本站提供，来源、许可和衍生哈希见 `assets/manifest.json`。

历史 r6–r12 报告与旧发布目录保留。一份旧硬编码检查器曾把 r13 结果写入 fix2 命名的焦点像素报告；该文件不能再作 fix2 历史证据。实际情况和修正见 `reports/acceptance/r13-legacy-report-note.json`，当前使用 `r13-focal-pixels.json`。
