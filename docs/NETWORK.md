# 代理访问排查

本站运行于现有 California 服务，入口为 https://daguanyuan-rumeng.zeabur.app/ 。网页、模型、纹理和接口均为同源资源。

2026-09-24 本机 Clash Verge 开启 TUN、规则模式。系统 DNS 返回 `198.18.0.234`，这是 fake-IP 占位，不能据此认定公网 DNS 错误。最初经域名连接在 TLS 握手阶段失败，尚未进入网页。公开 DNS 查询返回 `47.89.212.251`；保留域名和证书校验、以该地址建立连接则成功。

针对本站添加 DIRECT 后可返回 200，但国内直达 California 的线路较慢：`overview-low.glb` 的压缩传输为 2,867,205 字节，20 秒只收到 542,106 字节。继续测试本站的解析覆盖并保留原有代理组后，同一资源完整下载约 3.38 秒。这些是本机单次观测，不是所有网络的性能保证。

普通浏览器还出现本站 HTTP/3（UDP）资源传输迟缓，仅修正解析仍未在120秒内载入完整场景。因此当前订阅对本站增加一条 UDP 拒绝规则，使浏览器自动回退至 TCP；不在浏览器全局关闭 HTTP/2 或 HTTP/3。最终普通浏览器和显式代理分别在12.34／9.41秒完成桌面／手机模拟园景加载，无页面或资源错误，正常验证 TLS。

本机修复只对本站域名设置 Clash hosts 映射，并在当前订阅前置规则中设置本站 UDP 回退及原有代理组。没有修改 Windows hosts、系统 DNS、代理节点、其他域名规则或关闭 TLS 校验。原配置备份保存在不入库的 `.checkpoints/proxy-site-20260924/`。此映射适用于当前服务器；迁移服务或域名解析变化时必须先核验新的公开 DNS，再更新或删除该域名映射，不能沿用旧地址。切换其他订阅也需重新检查分流配置。

验证命令：

```powershell
$env:GARDEN_TEST_PROXY='http://127.0.0.1:7897'
npm run test:network
```

该检查在普通浏览器和显式 HTTP 代理下打开实际网站，等待完整三维场景，检查健康接口与资源错误，保留桌面和触摸模拟截图。它不固定浏览器 DNS、不关闭 HTTP/2 或 HTTP/3，也不跳过证书验证。HTTP 握手失败发生在应用之前，修改 React 页面不能修复该类故障；应先检查解析、代理线路和 TLS，再检查页面与资源加载。

## 2026-09-24 · macOS 与服务器复核

上文的 Windows 修复是历史验收，不能代表所有设备。本次在 macOS 重新检查：公网 A 记录为 `47.89.212.251`，无 AAAA；正常校验证书的 HTTP/1.1、HTTP/2 均可访问。Zeabur 使用共享自有 ingress-controller 1.3.2，非 nginx。尝试由应用发送 `Alt-Svc: clear` 后，网关仍同时附加 HTTP/3 宣告；该方案不能可靠关闭本站 HTTP/3，已撤回并重新部署，未改动共享网关或其他服务。

本机旧全局增强脚本仅把本站设为 DIRECT，造成 California 直连较慢。现仅针对完整域名，使用当前订阅 MATCH 指向的现有代理组、已核实的公网地址映射，以及本站 UDP 拒绝规则。增强脚本随订阅更新保留，原文件备份于 `.checkpoints/proxy-mac-20260924/`；结构对比确认其他配置未变。迁移服务器时需更新或移除这个域名映射。

最终顺序下载同一 `overview-low.glb`：直连 75.80 秒，普通域名 HTTP 代理 3.28 秒；传输 2,867,205 字节，解压后 SHA-256 均与发布文件一致，均正常验证 TLS。代理响应出口国家为 US，直连为 CN。该数据仅为本机单次测量。另有 CONNECT 到公网 IP 的诊断测试，不能当作普通浏览器验收。

已合并伙伴的轻量阅读、数据重试与场景失败入口；`/reading.html` 不依赖脚本或三维模型。用户暂无自有域名，本次未接入 CDN、购买服务器或修改全局网络配置。客户端规则能修复当前代理环境，但服务器无法保证所有代理的解析、屏蔽规则与出口线路均可用。当前证据见 `reports/acceptance/network-server-20260924/`；其中 `local-http.json` 仅记录已撤回响应头方案的本地实验，不能作为最终线上配置说明。
