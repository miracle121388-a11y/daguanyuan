disposition: fix

Scope: fresh, read-only finish review of the participation extension. No approved comp or decision comp applies to this inherited paper/jade extension. All 15 named captures were opened at their original sizes. Source review sampled the listed application files and tests; no browser, test, build, second detector, credentials, or deployment was run by this reviewer. DESIGN.md and its sidecar were reviewed for the incumbent visual roles, not revalidated as a whole-product history.

## persistence

Pass. PRODUCT.md, DESIGN.md, `.impeccable/design.json`, and `.impeccable/simulation-participation-surface.md` exist. The direction and quality bar describe an extension of the incumbent garden and handbook. No visual-world replacement, comp approval, new raster asset, or new concept roll is required for this bounded extension.

The required evidence set is complete and capture-valid: the local packet's `desktop-compose`, `desktop-chat`, `desktop-choices`, `desktop-choice`, `desktop-invitation`, `desktop-gathering` at 1440×900; `mobile-reply` and `mobile-chat` at 390×844; `phone-320-chat` and `phone-320-gathering` at 320×740; and the model packet's `desktop-compose`, `desktop-chat`, `mobile-reply`, `mobile-chat`, and `phone-320-chat` at the corresponding sizes. The captures contain the named interface states and rendered garden, without blank or failed rendering regions. Internal handbook scrolling is visible in the named content-state captures.

`src/simulation/participation.ts:8–20,56–68,92–132` and `src/simulation/store.ts:43–47,124–176` show complete moment snapshots, persistent journal commits, bounded conversation effects, fork/restore support, invitation cancellation, and late-response cancellation guards. This is source evidence. The two supplied `report.json` files report 21 local checks and 12 model checks passed, with two model conversation HTTP 200 responses; this reviewer did not rerun them. The supplied detector file contains 47 font-size and 3 color advisories, all advisory severity. Its incumbent typography literals do not authorize changing the global design files. The later 35-check regression report was announced by the parent but was not opened in this review.

## fidelity

| Contract element | Classification | Examined evidence |
| --- | --- | --- |
| TYPE | Match | Literary names and responses retain the device serif voice; operation labels retain the Chinese sans-serif voice. The reply scale is distinct from metadata in both provider packets. |
| MATERIAL | Match | Existing modeled courtyards and code-native people remain the subject. Paper reading surfaces, fine rules, Lucide icons, and restrained jade actions introduce no imitation physical texture or decorative image requirement. |
| GROUND | Match | The extension uses the incumbent paper, paper-light, ink, jade, and cinnabar variables in `simulation.css:225–243`; the visible grounds remain consistent with the supplied paper/jade identity. No comp pixel comparison is claimed. |
| Selected person, garden, and direct participation entry | Match | Desktop compose captures show the person and courtyard beside the handbook. The scene names the selected person and offers “交谈入局”; the panel exposes all three modes. `participate` selects the person and close camera. |
| Phone composition | Acceptable adaptation | 390px and 320px captures retain a visible modeled garden above the expanded handbook, as the surface brief requires. The composer and mode controls stay within the panel; 320px needs vertical scrolling to reach the send row. The CSS keeps mobile inputs at 16px and the established expandable panel provides space. |
| Continuous conversation and bounded personal effects | Match | Current turns visibly identify local rules or DeepSeek. `conversationContext` limits history to this person's last four turns and personal memories; `recordConversation` validates evidence IDs and applies fixed, bounded effects. No model-prose state patch is accepted by that function. |
| Response source and evidence throughout persisted history | Missing | `SimulationParticipation.tsx:29` renders earlier turns using only player text and reply. On the fourth turn, the oldest response loses its provider label, outcome, and memory disclosure in the conversation UI, although those fields remain stored. See F1. |
| Conversation recovery in the working viewport | Missing | `SimulationPanel.tsx` places the error alert and cancel control before the record tabs and transcript. The compose captures show that region scrolled away. `Conversation` displays a waiting label but has no local cancel/error message, and a failed `converse` only updates the global error. See F2. This is a source-derived failure-state finding, not a claim that the successful capture failed. |
| Choices and reversible moments | Match | Choice captures show consequences before selection, a separate fork action, a saved outcome, a restore action, and explicit wording that the writing request is queued. Source queues an actual directive and preserves the original branch. |
| Invitation, arrival, and completed activity | Match at reviewed scope | Invitation capture says people are waiting to travel; completed captures show all four at the modeled courtyard and shared-experience status. The local acceptance report separately records arrival before activity rewards and persistence after reload. The reviewer did not execute those transitions. |
| New primary action sizing | Contradicted | The quality bar requires primary actions of at least 44px. The desktop scene entry is explicitly 42px in `simulation.css:229`; the participation continuation/invitation controls inherit 42px from `.sim-branch-button` at line 39. Mobile overrides reach 44px. See F3. |
| Literature and staged simulation remain distinct | Match at reviewed scope | The persistent footer labels fictional simulation. The panel explanation separates original evidence from staged people, routes, and dialogue; conversation memory disclosures are called experiences rather than original-text citations. No new historical claim or external runtime asset was found in the sampled extension. |

## ceiling

The inherited visual direction is reached at the scope of this extension: scene depth and actor placement supply the experience; the handbook supplies reading order and task controls. No new ornamental frame, display-face replacement, raster treatment, or decorative motion is owed by this contract. The mobile inputs, clear focus treatment, wrapped controls, and contained panel are visible or source-supported. The remaining work is the bounded provenance, recovery, and sizing list below. This does not approve the whole garden, all screen-reader behavior, real-device keyboard behavior, or all application states.

## material_fixes

1. **F1 · P2 · Continuous-conversation evidence contract:** Preserve each earlier turn's provider label, outcome, and expandable memory evidence in the `turns.slice(0, -3)` branch at `src/panels/SimulationParticipation.tsx:29`, preferably using the same turn renderer as recent replies; keep the earlier-history disclosure compact. Verify four or more turns with mixed providers so expanding history still identifies who generated every response and which remembered experiences support it.
2. **F2 · P2 · Busy/error/recovery quality bar:** Put a conversation-scoped failure/cancellation message and a working cancel action beside the active reply/composer in `src/panels/SimulationParticipation.tsx:36–43`; retain the draft on failure/cancel. The global controls in `SimulationPanel.tsx` are above the scroll position shown by the compose captures, so a rejected remote reply currently returns the visible composer to idle without a visible explanation. Verify the failure and cancellation paths while the mobile composer remains in view, with clear recovery and without duplicate live announcements.
3. **F3 · P2 · Primary-action quality bar:** Raise the new desktop “交谈入局” action and participation continuation/invitation actions to at least 44px by scoping the changes to `.sim-stage-actions button:first-child` and `.sim-participation .sim-branch-button`; current 42px declarations are at `src/styles/simulation.css:229` and `:39`. Preserve the existing mobile sizing and inherited global design files.

## keep

Keep the modeled garden visible, the paper/jade/serif identity, the three direct participation modes, source-labeled fictional dialogue, pre-action consequences, exact snapshot reversal, and arrival-before-shared-results behavior.
