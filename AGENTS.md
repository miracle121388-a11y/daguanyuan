# Project rules
Implement the supplied Daguanyuan brief. Preserve existing work. The user has now explicitly authorized deploying this project to their existing Zeabur California server after refinement and mobile optimization. Do not purchase a new server or alter unrelated services. Keep literature evidence distinct from spatial interpretation and staged navigation. Never claim an unrun test passed.
Progress: docs/PROGRESS.md. npm scripts are the build contract. Models are generated by Blender using config/garden.layout.json. Generated collections are replaceable; Manual_Adjustments is preserved.
Use local resources at runtime. Data published from reviewed data/canon only. Sources and licenses require provenance and hashes.
